# Task 8.5 Plan Request — Empty State Prompt for Roadmap

## Goal
Replace the empty roadmap message on the client detail page with a clearer prompt to add a roadmap.

## Requirements
- Update the empty state copy to a CTA-style prompt (e.g., “No roadmaps yet. Add one to get started.”)
- Keep styling consistent with existing EmptyState component
- No behavior changes, just copy

## Token Hygiene
Reminder: For implementation, skip running tests unless user explicitly asks. Note any lint/test failures as pre‑existing.
