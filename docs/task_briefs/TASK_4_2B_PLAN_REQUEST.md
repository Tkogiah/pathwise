# Task 4.2b Plan Request — Overlay Token

Please produce a concrete plan for **Task 4.2b** only. This should be a step‑by‑step proposal **without implementing yet**.

## Context Docs

- `docs/DESIGN_TOKENS_BRIEF.md`
- `docs/task_briefs/TASK_4_2_PLAN.md`

## Scope

- Add a **theme‑safe overlay token** for drawer backdrops.
- Define explicit light + dark overlay values in tokens.css.
- Map into Tailwind via @theme as `bg-overlay` (or equivalent).
- Keep token system the single source of truth (no hard‑coded overlay colors in components).

## Deliverable

- A short plan (5–10 steps max).
- The token names + how to map into Tailwind.
- Where overlay is used (TaskDrawer backdrop).
