# Task 3.2 Plan — Responsive Improvements

## Scope

- Improve tablet/laptop layout without changing mobile baseline

## Steps

1. Widen container on large screens (`max-w-3xl` → `lg:max-w-5xl` in layout)
2. Increase StageNode width on md (`w-20` → `md:w-24`)
3. Increase StageNode text size on md (title + progress)
4. Make ClientRoadmapShell header responsive (stack on small, row on sm+)
5. Increase TaskDrawer width on lg (`md:max-w-md` → `lg:max-w-lg`)
6. Verify build/lint/format/test

## Notes

- Mobile-first preserved
- Only responsive class additions
