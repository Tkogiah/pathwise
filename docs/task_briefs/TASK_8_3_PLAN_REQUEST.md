# Task 8.3 Plan Request — Frontend Auth UI + Guard

## Goal

Add login/register pages and protect app routes for demo.

## Requirements

- /login and /register pages
- Store JWT in localStorage
- Auth guard for app routes (redirect to /login)
- Basic logout button
- Demo user selector still usable post‑login (optional)

## Token Hygiene

Reminder: For implementation, skip running tests unless user explicitly asks. Note any lint/test failures as pre‑existing.
