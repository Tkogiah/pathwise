# Task 4.3 Plan Request — ProgressArc Component + Placement

Please produce a concrete plan for **Task 4.3** only. This should be a step‑by‑step proposal **without implementing yet**.

## Context Docs

- `docs/UI_CLEANUP.md`
- `docs/DESIGN_TOKENS_BRIEF.md`

## Scope

- Implement a ProgressArc (thick gauge arc) component using SVG.
- Use it for:
  - Overall client progress (header)
  - Selected stage progress (zoom-in)
- Use token-based colors (accent + semantic status as appropriate). No custom hex in components.

## Deliverable

- A short plan (5–10 steps max).
- Component API (props, sizes).
- Where it will be placed and how progress is computed.
- Any risks/edge cases.
