# Pathwise local development environment
# Copy this file to .env: cp .env.example .env

# Database connection (used by Prisma)
DATABASE_URL=postgresql://pathwise:pathwise@localhost:5432/pathwise_dev

# Optional: change if port 5432 is already in use locally
# POSTGRES_PORT=5433
